diff --git a/ai/api/app.py b/ai/api/app.py
index 948fc0b..f962108 100644
--- a/ai/api/app.py
+++ b/ai/api/app.py
@@ -39,10 +39,15 @@ class SQLEditInputData(BaseModel):
     tableInfo: Optional[str] = None
     modelId: Optional[str] = None
     openaiApiKey: Optional[str] = None
+    openaiApiBase: Optional[str] = None
 
 @app.post("/v1/stream/sql/edit")
 async def v1_steam_sql_edit(data: SQLEditInputData, _ = Depends(get_current_username)):
-    llm = initialize_llm(model_id=data.modelId, openai_api_key=data.openaiApiKey)
+    llm = initialize_llm(
+        model_id=data.modelId,
+        openai_api_key=data.openaiApiKey,
+        openai_api_base=data.openaiApiBase,
+    )
     chain = create_sql_edit_stream_query_chain(llm, data.dialect, data.tableInfo)
 
     async def generate():
@@ -58,11 +63,16 @@ class PythonEditInputData(BaseModel):
     variables: str
     modelId: Optional[str] = None
     openaiApiKey: Optional[str] = None
+    openaiApiBase: Optional[str] = None
 
 
 @app.post("/v1/stream/python/edit")
 async def v1_stream_python_edit(data: PythonEditInputData, _ = Depends(get_current_username)):
-    llm = initialize_llm(model_id=data.modelId, openai_api_key=data.openaiApiKey)
+    llm = initialize_llm(
+        model_id=data.modelId,
+        openai_api_key=data.openaiApiKey,
+        openai_api_base=data.openaiApiBase,
+    )
     chain = create_python_edit_stream_query_chain(llm)
 
     async def generate():
diff --git a/ai/api/llms.py b/ai/api/llms.py
index 5a1cc50..5a6588b 100644
--- a/ai/api/llms.py
+++ b/ai/api/llms.py
@@ -16,7 +16,7 @@ def str_to_bool(value):
 
     return value.lower() in ['true', '1', 't', 'y', 'yes']
 
-def initialize_llm(model_id=None, openai_api_key=None):
+def initialize_llm(model_id=None, openai_api_key=None, openai_api_base=None):
     openai_api_key = openai_api_key or config("OPENAI_API_KEY")
     use_azure = config("USE_AZURE", default=False, cast=str_to_bool)
 
@@ -37,11 +37,19 @@ def initialize_llm(model_id=None, openai_api_key=None):
             api_version=config("AZURE_API_VERSION", default=""),
         )
     else:
-        # Initialize OpenAI using environment variables for API key and model name
+        # Resolution order for the OpenAI base URL:
+        #   1. per-workspace setting (openai_api_base arg, sent by api)
+        #   2. OPENAI_BASE_URL / OPENAI_API_BASE env (container default)
+        #   3. SDK default (https://api.openai.com/v1)
+        base_url = openai_api_base or config(
+            "OPENAI_BASE_URL",
+            default=config("OPENAI_API_BASE", default=None),
+        )
         llm = ChatOpenAI(
             temperature=0,
             verbose=False,
             openai_api_key=openai_api_key,
             model_name=model_id if model_id else config("OPENAI_DEFAULT_MODEL_NAME"),
+            base_url=base_url,
         )
     return llm
